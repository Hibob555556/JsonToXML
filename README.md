# TypescriptAutodoc

A Nuxt 4 application with a Dragon Editor demo page, TypeScript support, and a testing setup for unit, Nuxt, and end-to-end coverage.

This repository is currently centered around an editor route at `/editor`, rendered from the Nuxt 4 `app/` directory structure.

## Stack

- Nuxt 4
- Vue 3
- TypeScript
- Dragon Editor
- Nuxt UI
- Nuxt Content
- Vitest
- Playwright

## Requirements

- Node.js 20 or newer is recommended for current Nuxt 4 tooling
- npm is the expected package manager in this repository because `package-lock.json` is committed

## Getting Started

Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Nuxt will usually start on `http://localhost:3000`. If that port is already in use, it will automatically choose another one such as `http://localhost:3001`.

## Available Scripts

### App lifecycle

```bash
npm run dev
npm run build
npm run preview
npm run generate
```

- `npm run dev`: start the local development server
- `npm run build`: create a production build
- `npm run preview`: preview the production build locally
- `npm run generate`: generate a static build when applicable

### Testing

```bash
npm run test
npm run test:watch
npm run test:coverage
npm run test:unit
npm run test:nuxt
npm run test:e2e
npm run test:e2e:ui
```

- `npm run test`: run Vitest
- `npm run test:watch`: run Vitest in watch mode
- `npm run test:coverage`: run Vitest with coverage output
- `npm run test:unit`: run unit tests from `test/unit`
- `npm run test:nuxt`: run Nuxt-aware tests from `test/nuxt`
- `npm run test:e2e`: run Playwright tests from `tests`
- `npm run test:e2e:ui`: open Playwright UI mode

## Project Structure

```text
.
|-- app/
|   |-- app.vue
|   `-- pages/
|       `-- editor/
|           `-- index.vue
|-- public/
|-- test/
|   |-- nuxt/
|   `-- unit/
|-- tests/
|-- nuxt.config.ts
|-- package.json
|-- playwright.config.ts
`-- vitest.config.ts
```

## Routes

- `/editor`: Dragon Editor demo page defined in [app/pages/editor/index.vue](./app/pages/editor/index.vue)

## Nuxt App Layout Notes

This project uses the Nuxt 4 `app/` directory structure:

- Root application shell lives in [app/app.vue](./app/app.vue)
- Route components should live under `app/pages/`
- If `app/app.vue` renders `<NuxtPage />`, Nuxt expects page components to exist in `app/pages/...`

If you place route files in a top-level `pages/` directory while also using `app/app.vue`, Nuxt may warn that no page component exists for `<NuxtPage>`.

## Current Editor Page

The current editor demo is intentionally simple:

- It renders a `DragonEditor` component
- It stores editor content in a local Vue ref
- It is a good starting point for integrating persistence, toolbar controls, schema extensions, or export features

## Configuration

Main project configuration lives in:

- [nuxt.config.ts](./nuxt.config.ts)
- [vitest.config.ts](./vitest.config.ts)
- [playwright.config.ts](./playwright.config.ts)

Nuxt modules currently configured include:

- `@nuxt/ui`
- `@nuxt/image`
- `@nuxt/content`
- `@nuxt/eslint`
- `@nuxt/hints`
- `@nuxt/test-utils`
- `@nuxtjs/device`
- `@nuxtjs/eslint-module`
- `@nuxtjs/seo`
- `dragon-editor`
- `nuxt-api-shield`
- `nuxt-auth-utils`
- `nuxt-authorization`
- `nuxt-charts`

## Environment Variables

Local environment files are supported:

- `.env`
- `.env.local`
- `.env.development`
- `.env.production`

Do not commit real secrets. The repository ignores local env files by default.

## Build and Deployment

Create a production build:

```bash
npm run build
```

Preview the production server locally:

```bash
npm run preview
```

For deployment, use the output generated by Nuxt for your chosen hosting platform. Refer to the Nuxt deployment docs for platform-specific guidance:

- https://nuxt.com/docs/getting-started/deployment

## Development Notes

- If the app shows the Nuxt welcome screen instead of your route, check [app/app.vue](./app/app.vue) and make sure it renders `<NuxtPage />`
- If `/editor` does not render, confirm the file exists at `app/pages/editor/index.vue`
- If the dev server switches ports, use the exact local URL shown in the terminal

## Next Improvements

Good follow-up tasks for this repository:

- Add typed editor content models instead of loose arrays
- Display a live JSON preview of editor state
- Add save and load behavior
- Add real unit tests for editor behavior
- Add end-to-end coverage for the `/editor` flow
- Document environment variables once auth or API features are wired in

## License

Add a license section here once you decide how this project should be shared.
